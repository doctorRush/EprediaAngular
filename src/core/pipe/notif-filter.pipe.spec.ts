import { NotifFilterPipe } from './notif-filter.pipe';

describe('NotifFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new NotifFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
