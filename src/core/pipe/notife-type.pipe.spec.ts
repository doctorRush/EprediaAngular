import { NotifeTypePipe } from './notife-type.pipe';

describe('NotifeTypePipe', () => {
  it('create an instance', () => {
    const pipe = new NotifeTypePipe();
    expect(pipe).toBeTruthy();
  });
});
